{
  "version": "0.0.1",
  "devDependencies": {
    "lerna": "^3.19.0",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">= 12.x"
  },
  "precommit": "lerna run --concurrency 1 --stream precommit",
  "scripts": {
    "deploy": "lerna bootstrap --npm-client=yarn && lerna publish",
  }
}
