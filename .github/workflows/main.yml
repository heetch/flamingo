name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Install dependencies
      run: npm install --prefix packages/react

    - name: Prettier
      run: npm run prettier --prefix packages/react

    - name: Linting
      run: npm run lint --prefix packages/react
